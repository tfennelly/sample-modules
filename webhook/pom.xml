<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
    <groupId>com.foxweave.components</groupId>
    <artifactId>foxweave-component-webhook</artifactId>
    <version>0.1</version>
    <name>FoxWeave Webhook Component Module Example</name>

    <!--
        All the standard (core FoxWeave) components are provided for the runtime.  You will
        need to add any external dependencies though...
    -->
    <dependencies>
        <dependency>
            <groupId>com.foxweave</groupId>
            <artifactId>foxweave-api</artifactId>
            <version>0.1</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.json</groupId>
            <artifactId>json</artifactId>
            <version>20090211</version>
            <scope>provided</scope>
        </dependency>
	</dependencies>

    <!--
        The FoxWeave API is currently in a private maven repo on foxweave.com, so we need to
        add that repo here...
    -->
    <repositories>
        <repository>
            <id>foxweave-releases</id>
            <url>http://maven.foxweave.com/releases/</url>
        </repository>
    </repositories>

    <build>
        <plugins>
            <!--
                Produce Java 1.6 compatible bytecode...
            -->
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>2.3.2</version>
                <configuration>
                    <source>1.6</source>
                    <target>1.6</target>
                </configuration>
            </plugin>
            <!--
                Include the assembly plugin and point it to the assembly.xml file
                in the project...
            -->
            <plugin>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>2.3</version>
                <configuration>
                    <descriptors>
                        <descriptor>assembly.xml</descriptor>
                    </descriptors>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
